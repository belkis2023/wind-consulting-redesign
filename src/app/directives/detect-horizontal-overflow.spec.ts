import { DetectHorizontalOverflow } from './detect-horizontal-overflow';

describe('DetectHorizontalOverflow', () => {
  it('should create an instance', () => {
    const directive = new DetectHorizontalOverflow();
    expect(directive).toBeTruthy();
  });
});
