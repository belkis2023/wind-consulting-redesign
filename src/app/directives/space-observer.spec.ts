import { SpaceObserver } from './space-observer';

describe('SpaceObserver', () => {
  it('should create an instance', () => {
    const directive = new SpaceObserver();
    expect(directive).toBeTruthy();
  });
});
