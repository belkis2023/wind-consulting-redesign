import { GlobeProjectsScroll } from './globe-projects-scroll';

describe('GlobeProjectsScroll', () => {
  it('should create an instance', () => {
    const directive = new GlobeProjectsScroll();
    expect(directive).toBeTruthy();
  });
});
