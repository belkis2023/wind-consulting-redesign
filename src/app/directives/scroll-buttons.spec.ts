import { ScrollButtons } from './scroll-buttons';

describe('ScrollButtons', () => {
  it('should create an instance', () => {
    const directive = new ScrollButtons();
    expect(directive).toBeTruthy();
  });
});
