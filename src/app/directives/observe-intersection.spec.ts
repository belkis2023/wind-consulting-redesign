import { ObserveIntersection } from './observe-intersection';

describe('ObserveIntersection', () => {
  it('should create an instance', () => {
    const directive = new ObserveIntersection();
    expect(directive).toBeTruthy();
  });
});
