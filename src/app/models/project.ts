export interface project {
    title: string,
    description: string,
    backgroundImageUrl: string,
    tags: string[]
}

export interface chiffreCle {
    number: string,
    type: string,
    description: string,
}
